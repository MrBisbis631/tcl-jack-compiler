# Transpile parsed XML doc to vm code

source "[file normalize .]/jack-2-vm/symbol-table.tcl"
source "[file normalize .]/jack-2-vm/pointers.tcl"
source "[file normalize .]/jack-2-vm/code-generators.tcl"

# Transpile a single jack file to vm code
proc jack_to_vm {doc} {
  # TODO implement
}

# Transpile a jack file to vm code in a generator style using a XML parser
proc jack_to_vm_generator {xml_parser} {
  # TODO implement
}
